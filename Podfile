# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LendAHand' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LendAHand

  target 'LendAHandTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LendAHandUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  # pod 'Reusable'
  
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

  pod 'FacebookCore'
  pod 'FacebookLogin'
  pod 'FacebookShare'


end
